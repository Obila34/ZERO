[Unit]
Description=ZERO Vision server - depth + scene facts (GPU)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=CHANGEME
WorkingDirectory=/home/CHANGEME/ZERO/server/vision
ExecStart=/home/CHANGEME/ZERO/.venv/bin/uvicorn app:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
